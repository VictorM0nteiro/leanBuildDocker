# leanBuildDocker Report

Generated by `lbd`. This document explains every non-trivial decision the tool made when constructing your Dockerfile.

## Summary

| Item | Value |
| --- | --- |
| Build image | `{{ .BuildStage.BaseImage }}` |
| Runtime image | `{{ .RuntimeStage.BaseImage }}` |
| Binary | `{{ .BinaryPath }}` |
| Entry command | `{{ execForm .RuntimeStage.EntryCommand }}` |
{{- if .RuntimeStage.ExposedPorts }}
| Exposed ports | {{ range $i, $p := .RuntimeStage.ExposedPorts }}{{ if $i }}, {{ end }}`{{ $p }}`{{ end }} |
{{- end }}
{{- if .RuntimeStage.Assets }}
| Runtime assets | {{ range $i, $a := .RuntimeStage.Assets }}{{ if $i }}, {{ end }}`{{ $a.Src }}`{{ end }} |
{{- end }}
{{- if .RuntimeStage.Env }}
| Runtime env | {{ range $i, $e := .RuntimeStage.Env }}{{ if $i }}, {{ end }}`{{ $e.Key }}={{ $e.Value }}`{{ end }} |
{{- end }}
| Multi-stage | {{ if .UseMultiStage }}yes{{ else }}no{{ end }} |
| BuildKit cache | {{ if .UseCacheMounts }}yes{{ else }}no{{ end }} |

## Decisions
{{ range .Decisions }}
### {{ .Topic }} — {{ .Chose }}

{{ .Because }}
{{ if .Alternatives }}
**Alternatives considered:**

{{ range .Alternatives }}- {{ . }}
{{ end }}{{ end }}{{ end }}
